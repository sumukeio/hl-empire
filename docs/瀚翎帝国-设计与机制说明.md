# 瀚翎帝国：统一之战 — 设计与机制说明

本文档描述当前实现中的**产品隐喻**、**功能机制**、**状态与持久化**、**UI 信息架构与视觉**，便于后续迭代或与 `docs/MVA.md`、`docs/锚定.md` 等业务文档对照。

**近期修订索引**：机制细节以 **§3–§6** 正文为准；**本轮增量（约 2026-05）** 见下节（含 **巡游四海 M1**、集团军、勤政录等）。

### 近期实现更新（约 2026-05）

#### 仪表盘与军机（既有）

- **中央沙盘 · 态势筛选**（`components/dashboard/war-map.tsx`）：沙盘标题区提供四类态势（未知之地 / 斥候侦查 / 战火纷飞 / 金色藩属）**多选**；**默认仅选中「战火纷飞」**；可点选多个或取消；**全部取消**时视为**不筛选**、展示全部城池；网格仅渲染 **`getTerritoryCities`** 征战目标（**不含通务司**）；当前打开的奏折若不在可见城内则 **自动关闭 Sheet**。
- **军机处 · 点卯交互**（`components/dashboard/quest-engine.tsx`）：可多次勘合的任务使用 **「点卯 / 再办一次」** 主按钮（满档为「本日已办满」并禁用），**不再**以复选框半选/全选为主路径；列表行展示 **「功勋 +N（折算券）」**（按每 **50** 功勋约 **1** 翻牌券，与内务府蓄池规则一致）。`toggleQuest` 成功时返回 **`ToggleQuestCompletionMeta`**（`store/quest-store.ts`），供邸报 **`revert`** 写入精确回滚字段。
- **翻牌券 · 多巴胺池**（`store/emperor-store.ts`）：**`dopaminePool`（0–49，持久化）** 与 **`DOPAMINE_ENERGY_PER_TICKET = 50`**（`DOPAMINE_POOL_MAX = 49`）；军机点卯在 **`addExp`** 之后调用 **`feedDopamineFromQuestReward(expGain)`**：将本笔功勋注入池，按 **⌊(池内余量 + E) / 50⌋** 张券记入 **`tokens`**，余量写回 **`dopaminePool`**；铸出至少 1 券时邸报 **`treasury`** + **`goldFlash`**。**不再**每点卯固定 `addTokens(1)`。邸报 **`revert`** 含 **`postDopaminePool` / `dopamineExpFed` / `tokensSubtracted`**；**`lib/apply-event-log-revert.ts`** 撤回前校验池与记录一致。**内务府**（`neiwufu-cabinet.tsx`）：蓄池 Progress + **`animate-imperial-token-pop`**（`tailwind.config.ts`）。
- **勤政录 / 邸报 · 云端**（约 2026-05 增量，见下节 **「勤政录 / 邸报 · Supabase」**）：顶栏 **日历** → **`/dashboard/activity`**；顶栏邸报默认 **北京今日**；工时与邸报分表永久保留。
- **勤政录（早期）**：日 / 周 / 月过滤 + Markdown/JSON 导出（`lib/activity-report.ts`）；密函含 **`dopaminePool`**；**`client_schema_version` 2** 含 **`grandTour`** 与云列 **`grand_tour_json`**。

#### 通务司 · 政务归属与四维（2026-05 增量）

- **通务司城池**（`lib/tongwu-si.ts`，`TONGWU_SI_CITY_ID`）：疆域中固定一条 **非沙盘征战目标** 的「通务司」记录；**九州图志**仅展示 **`getTerritoryCities`**，通务司不出现在沙盘网格。
- **政务归属 `affiliation`**（`store/types.ts` · `QuestAffiliation`：`city` | `tongwu`）：军机处主攻选 **通务司** 时仅显示 **天下通务** 条目；选 **征战目标** 时仅显示 **分城政务**（`lib/quest-affiliation.ts` · `filterQuestsForActiveCity`）。通务司在主攻下拉中 **置顶**；点卯计时中 **不可切换** 主攻。
- **业务四维 `category`**（`product` / `traffic` / `conversion` / `delivery`）：枢密院可改；军机处支持按维度筛选（`lib/quest-category.ts`）。
- **枢密院 UI**（`settings-view.tsx`）：**「分城政务 / 天下通务」** 双 Tab 维护模板；每条可设 **战役阶段**、归属、维度、最短耗时等。
- **启动收尾 `ensureQuestBootstrap()`**（`store/quest-store.ts`，`EmpireCloudSync` 调用）：**`ensureTongwuSiCity()`** + **`pruneQuestProgressForUnknownIds`** + 校正无效 **`activeCityId`**；**不再**在启动时强制整表替换为 MVA 三十一务（用户删改/自建条目保留）。**`ensureMvaQuestCatalog`** 已 **deprecated**，行为等同 **`ensureQuestBootstrap`**。新档或造办处仍可用 **`createDefaultQuests()`** 一次性写入默认清单。

#### 集团军 · 战役集群（2026-05 增量）

- **独立子路由**（`app/dashboard/campaign/page.tsx`，`components/dashboard/campaign-ops-view.tsx`）：**`/dashboard/campaign`** — 与九州图志主页分离的 **集团军作战室**；顶栏 **Layers** 图标入口（`TreasuryHUD`，位于勤政录与造办处之间）；**`MobileSubpageShell`** 顶栏 **←** 返回 **`/dashboard`**。深链：`?phase=ON_ORDER`、`?cities=id1,id2`（可选预填战役模式与参战城）。
- **战役阶段 `CampaignPhase`**（`store/types.ts`，`lib/campaign-phase.ts`）：`PRE_LAUNCH` 战役筹谋 · `POST_LAUNCH` 合围据守 · `ON_LEAD` 短兵相接 · `ON_ORDER` 捷报破城。任务字段 **`campaignPhase?`**；未设时按 MVA id 表 / 标题关键词推断（`getQuestCampaignPhase`）；枢密院可覆盖。
- **批处理 UI**（`components/dashboard/batch-campaign-ops.tsx`）：步骤 A 选战役模式 → B **多选征战城池**（名 + 别名，可全选）→ C 按阶段过滤 **分城政务** 流水线；每条 **「集群点卯」**。
- **集群点卯逻辑**（`store/quest-store.ts` · `lib/batch-campaign.ts`）：**`startBatchCampaignQuest`** 遍历勾选城，跳过已满档 / 一次性已完成（`resolveBatchCampaignEligibility`）；一次性扣 **总体力**；**`activeBatchCampaign`** 计时与单机 **`activeTimer` 互斥**（持久化于 `hanling-quest`）。**`completeBatchCampaignWithTimer`**：各城 **`incrementQuestCompletion`** + 功勋 **`addExp`** 后，**一次性** **`feedDopamineFromQuestReward(合计功勋)`**；批量工时 **T_standard = minCompletionTime × N**，**T_floor = T_standard × 0.5**，与 **T_actual** 对比执行与单机点卯一致的多巴胺奖惩 / 迁延扣池（`applyQuestTimerOvertimePenalty`）。
- **破城连锁**：`ON_ORDER` 阶段完成标题匹配 **「开通学习系统」** 类任务（`isOpenLearningSystemQuest`）后，对每个成功城池写 **朱红加急** 邸报：「【工部告急】…请速执行【转运粮草】…」（`emphasis: crimsonDecree`）。
- **视觉**：作战室卡片 **`border-imperial-gold`** + **`animate-imperial-gold-glow`**，与日常军机处面板区分。

#### 巡游四海 · 行在舆图（2026-05 增量 · M1 已落地）

- **独立子路由**（`app/dashboard/grand-tour/page.tsx`，`components/dashboard/grand-tour-view.tsx`）：**`/dashboard/grand-tour`** — 真实旅行 **规划与记账**（与城池奏折内游戏化 **「巡幸疆土」** 分离：后者为一次性奖励，前者为可反复编辑的行在）；**`MobileSubpageShell`**；**小屏行在目录为 Sheet**（见文首「移动端 H5」）。
- **入口**：顶栏 **`Compass`**（`TreasuryHUD`，位于勤政录与集团军之间）；养正司 **理藩院** 首行 **「开卷舆图 · 巡游四海」**（`inner-court.tsx`）。
- **持久化**（`store/grand-tour-store.ts`，键 **`hanling-grand-tour`**）：**舆图库藏**（省/市京畿 + POI、常供名录）与 **行在列表**（`GrandTour` + `TourLeg[]`）分离；行程引用库藏时为 **快照**，改库藏不追溯旧行在。
- **京畿选择 · 三级折叠**（`components/dashboard/grand-tour-region-tree.tsx`，`lib/grand-tour-region-hierarchy.ts`）：**七大区**（华北 / 东北 / 华东 / 华中 / 华南 / 西南 / 西北）→ **省/直辖市**（如 北京、河北）→ **城市**（**古代雅称 · 现代名称**，如 **燕京 · 北京市**）。种子数据来自 **`docs/城市古今名称对照清单.md`**，运行时由 **`scripts/export-city-atlas-seed.py`** 生成 **`lib/city-atlas-seed.ts`**（361 市）；大区映射见 **`lib/city-macro-regions.ts`**。
- **行在（`GrandTour`）**：状态 **`draft` / `active` / `archived`**（归档只读）；标题默认按主京畿与日数生成、**御笔可改**（`titleCustomized`）；**不预填**日程，`legs` 靠 **「+ 本日添程」** 逐条追加。
- **行程条（`TourLeg`）**（`components/dashboard/grand-tour-itinerary.tsx`）：五程别 **游览 / 膳宿 / 驻跸 / 驿传 / 门券（`ticket`）**；四时辰 **早朝 / 晌午 / 傍晚 / 深夜**（与军机 `QuestPeriod` 同）；**日内拖拽**排序；**复制此程**；同日时辰 **`rank ≥ 上一条`**（允许重复时辰）；冲突 **不阻断** 保存，标 **「序时待勘」**（一键修复留 M2）。跨日改日用菜单 **「移至第 N 日」**。
- **舆图库藏 Tab**：京畿舆图（三级树 + POI CRUD）；常供名录（膳宿 / 驻跸 / 驿传 列表 CRUD）。删库藏项若仍有行在引用 → **提示**，不改旧快照。
- **用度总账**：自 `TourLeg` 汇总，单位 **「两」**（**1 两 = 1 元**，仅文案）；**M4** 再与 **`privateVault`** 差额联动。
- **帝国密函**（`lib/empire-backup.ts`）：可选字段 **`grandTour`**（`atlas` + `tours` + `activeTourId`），与 `localStorage` 双写。

**后续里程碑**（规划见 `docs/未来开发方向.md` §五）：M2 跨日拖 / 序时一键修复 / 朱批导入；M3 奏折导出；M4 内帑与启驾邸报。

#### 勤政录 / 邸报 · Supabase（2026-05 增量 · 已落地）

- **迁移**（`supabase/migrations/002_activity_journal.sql`，须在 `001_user_empire.sql` 之后执行）：
  - **`event_log`**：邸报叙事；`client_log_id` 幂等 upsert；`beijing_date` 供「今日」索引；`revert` jsonb 支持撤回。
  - **`quest_work_session`**：政务工时；**一行 = 一次办理周期**（单机或集团军 `session_kind`）；**`operations` jsonb 数组**记录 `timer_start` / `timer_pause` / `timer_resume` / `timer_cancel` / `complete` / `sop_complete` / `shoddy_void` / `batch_*` 等，每项含 ISO **`at`**。
  - **`user_empire.grand_tour_json`**：巡游四海快照，与密函 **`grandTour`** 同形。
- **时间**：展示与顶栏「今日」筛选用 **北京时间 `Asia/Shanghai`**（`lib/beijing-time.ts`）；邸报 UI 时间为 **`YYYY-MM-DD HH:mm:ss`**。
- **八百里加急**（`components/dashboard/event-log-docket.tsx`）：**仅叙事**；列表默认 **`isBeijingToday`**；**不含**工时明细。本地 `hanling-event` 缓存约 **500** 条；云端 **`event_log`** 长期保留。
- **勤政录**（`components/dashboard/activity-journal-view.tsx`）：**双 Tab** — **邸报摘录** / **政务工时**；日/周/月范围 + 导出（JSON 含 **`workSessions`**）。工时来自 **`useWorkSessionStore`**，登录后由 **`hydrateActivityJournalFromCloud`** 灌入。
- **写入路径**：点卯/集团军计时全程经 **`lib/work-session-recorder.ts`** → **`lib/activity-journal-bridge.ts`**；邸报仍 **`useEventStore.addLog`** → **`syncEventLogToCloud`**。每条计时携带 **`clientSessionId`**（`store/quest-store.ts` · **`ActiveQuestTimer`** / **`activeBatchCampaign`**）。
- **登录 bootstrap**（`components/empire-cloud-sync.tsx`）：`bindActivityJournalSync` → 拉取邸报 + 工时 → **`replaceLogsFromCloud`** / **`setSessions`**；五域仍 **`empire-sync`** 防抖写 **`user_empire`**（含 **`grand_tour_json`**，**`client_schema_version: 2`**）。
- **撤点卯**：无时间窗限制；撤点卯写入工时 **`timer_cancel` / `batch_cancel`**，并照常 **`addLog`** 叙事（若产品需要）。

#### 移动端 H5 · 子页面 M1（2026-05 增量 · 已落地）

- **公共能力**（`lib/mobile-ui.ts`）：**`touchTarget` / `touchTargetInline` / `touchInput`**（≥44px 触控）、**`mobilePageMain`**（底栏 **`safe-area-inset-bottom`**）、**`mobileStickyHeader`**（顶栏 **`safe-area-inset-top`**）。
- **子页面统一壳**（`components/layout/mobile-subpage-shell.tsx`）：**`←` 返回九州图志**、标题/副题、可选顶栏扩展；用于独立子路由，与主页 **`DashboardShell`** 底栏 Sheet 分工互补。
- **勤政录**（`activity-journal-view.tsx`）：**`MobileSubpageShell`**；双 Tab（邸报摘录 / 政务工时）与导出按钮 **小屏全宽**；日/周/月 **`type=date|month`** 输入 **`touchInput`**。
- **集团军**（`campaign-ops-view.tsx` + `batch-campaign-ops.tsx`）：作战室页 **统一壳**；战役模式 **Select**、参战城 **chip**、集群点卯/计时按钮 **≥44px**；**`< sm` 隐藏流水线拖拽把手**（大屏仍可拖序）。
- **巡游四海**（`grand-tour-view.tsx`）：统一壳；**`< lg` 行在目录收进左侧 Sheet**（顶栏 **「行在目录」** 按钮），**`lg+` 仍为左栏双栏**；Tab / 状态选择与行在列表项触控加高。
- **造办处**（`settings-view.tsx`）：顶栏 **安全区**；回朝 / 退出 / 帝国手册与 **图志司·枢密院·帝国档案** Tab **横向滚动 + 44px**。
- **主页 M1（既有）**：`TreasuryHUD` 小屏重排与 **44px** 顶栏图标；`QuestEngine` **`< lg` Sheet 选主攻城**；`app/layout.tsx` **`viewportFit: "cover"`**。详见 **`docs/移动端与Supabase适配方案.md`** §4 与 **`docs/实施步骤-登录与Supabase.md`** 第八步。
- **文案维护**：勤政录中文若遇编辑器编码损坏，可用 **`node scripts/write-activity-journal.mjs`**（依赖 **`scripts/activity-strings.b64.json`**）整文件再生。

---

## 1. 产品定位

- **形态**：单页仪表盘（SPA），技术栈为 Next.js 14 App Router + React。
- **叙事包装**：将「个人 / 业务上的最小可行性动作（MVA）」映射为**古风帝国经营**——皇帝属性对应个人状态大盘，城池对应业务单元或产品，军机处对应分时任务，邸报对应操作与系统反馈。
- **数据归属**：全部为**本地优先**；核心状态经 Zustand `persist` 写入浏览器 `localStorage`，无服务端账号体系（除非后续自行扩展）。

---

## 2. 技术栈与工程结构（与实现相关部分）

| 层级 | 选型 |
|------|------|
| 框架 | Next.js 14（App Router）、TypeScript |
| UI | Tailwind CSS、[shadcn/ui](https://ui.shadcn.com/)（New York / Zinc 基底）、Lucide 图标 |
| 状态 | Zustand，全部核心 Store 使用 **`persist` + `createJSONStorage(() => localStorage)`** |
| 入口页面 | `app/page.tsx` → `components/dashboard/dashboard-shell.tsx` |

**客户端恢复**：各 Store 配置 `skipHydration: true`，由 **`components/empire-cloud-sync.tsx`** 在挂载后 **`await rehydrateAllStores()`**（见 `store/rehydrate.ts`），再执行 **`resetDailyQuests()`** 与 **`ensureQuestBootstrap()`**（未登录 Supabase 时亦会执行后两步）。避免在 SSR 阶段访问 `localStorage`。

---

## 3. 状态管理与持久化

### 3.1 设计原则

- **按域拆 Store**：皇帝 / 地图 / 任务 / 事件 / **巡游四海** 等独立持久化，互不覆盖 `localStorage` 键名，便于调试与迁移。
- **合并策略（merge）**：皇帝档按 `exp` 重算 `level`；**地图 / 任务**以持久化数组为权威；旧档缺字段时由 `migrateCity` / `migrateQuest` 补默认值（如城池 `troops` / `equipments`）。疆域与军机初始列表由运行时逻辑注入（见 §3.3、§3.4），用户仍可在造办处清空或自定义。

### 3.2 `useEmperorStore`（皇帝 / 系统大盘）

| 字段 | 含义 | 约定 |
|------|------|------|
| `level` | 等级 | 由 `exp` 推导：`floor(exp/100)+1`，下限 1 |
| `exp` | 累计功勋（经验） | 以 `addExp` 等入口正向增加为主 |
| `stamina` | 体力 | 0–100；`consumeStamina` 扣减下限为 0 |
| `gold` | 国库银两 | 业务上可映射收入/现金等 |
| `troops` | 皇帝维度「帝国军力」摘要 | 与**单城池** `City.troops` 不同名同源隐喻 |
| `health` | 龙体健康 | 0–100，默认 100；`setHealth` / 安寝 / 内务府动作等会钳制 |
| `martialArts` | 武术 / 执行力 | 默认 **10**，`Math.max(0, …)` |
| `literature` | 文学修养（理藩院人文线） | 默认 **10**，`Math.max(0, …)` |
| `morale` | 民心 | 0–100，默认 100 |
| `privateVault` | 内帑 / 皇室私库（旅行基金等） | ≥0，默认 0 |
| `healthCombo` | 连续抗炎「连击」计数 | 御膳房 +1，毒丹清零；用于延寿丹邸报阈值 |
| `isNomadMode` | 移动行宫（游牧办公） | 默认 `false`；开启时军机 **仅 `quest-mva-*` 模板** 功勋结算 **×1.2**（见 `quest-store`） |
| `tokens` | 翻牌券 | 主要由军机 / 集团军功勋注入 **`dopaminePool`** 后按 **每满 50 点功勋铸 1 券**（`feedDopamineFromQuestReward`）；御花园 **`startEntertainment`** 每次消耗 **1** 券 |
| `dopaminePool` | 多巴胺蓄池余量 | **0–49**（`DOPAMINE_POOL_MAX`），与功勋结算同 persist；旧档缺省合并为 **0** |
| `isDressed` / `isEntertaining` / `entertainmentDeadline` / `entertainmentLowHealthPenalty` | 朝服与御花园娱乐态 | 见内务府奏折区 |

**内务府 / 养正司相关动作**（实现于 `emperor-store.ts`，多数会经 `useEventStore.getState().addLog` 写邸报）：

- **太医院**：`consumeAntiInflammatory`（体力+10、健康+1、连击；连击≥3 金色延寿邸报）、`trainMartialArts`（体力−5、武力+2）、`consumeJunkFood`（体力/健康惩罚、连击清零）。
- **户部**：`recordExpense(amount, 'infrastructure' \| 'decadence')`（基建扣实额；骄奢双倍扣金 + 民心−10）、`transferToPrivateVault`（国库→私库）。
- **理藩院**：`visitImperialArchives`（私库≥500）、`indulgeCommercialTourism`（私库≥1000）、`setNomadMode`。
- **宗人府**：`rejectInvalidSocial`、`meetGrandSecretariat`（需体力≥5）、`acceptEntropyCompanion`（私库腰斩、民心大损）。
- **户部司帑（顶栏国库 Dialog，亦属户部/内帑叙事）**：**`injectGold(amount)`** — 登记工资/补贴等岁入，`gold` 增加并邸报 `treasury`：「【内务府】岁入拨入 … 两。」；**`syncGold(total)`** — 将 `gold` 校准为实存整数并邸报：「【户部】国库储蓄已校准…」。另保留 **`updateGold`** 供内部与其它模块直接改写（工部拨款、奏折 CPA 联动等）。
- 另有：`sleepAtYangxinPalace`、`jungleHunt`、`royalSparring`、`startEntertainment`、`completeEntertainmentSession` 等宫务向动作。

**其它**：`addExp` 仅叠加功勋与尊号晋升检测（**不**写入多巴胺池）；**`feedDopamineFromQuestReward`** 专供军机点卯链路。**`dailyReset()`** 仍可将体力置满（宵禁整顿等）；跨日军机重置时见 **`resetDailyQuests`**（§3.4）按规则清理各城 **`completedQuestIds` / `questCompletedAt` / `questDailyCompletions`**（一次性任务进度永久保留）。

**持久化键名**：`hanling-emperor`；`partialize` 含 **`tokens`、`dopaminePool`** 等上述业务字段；`merge` 对缺省档补 `martialArts`/`literature` 默认 **10**、**`dopaminePool` 缺省为 0** 等。

### 3.3 `useMapStore`（疆域 / 业务产品大盘）

**城池结构**（`store/types.ts`）：

| 字段 | 说明 |
|------|------|
| `id` | 稳定唯一标识（新增时由 `createEmptyCity` 等生成） |
| `name` | 叙事名（如「长安」） |
| `alias` | 别名，对应实际产品 / 业务名（如「档案管理员」）；沙盘卡片在名称下展示，可为空 |
| `status` | `0 \| 1 \| 2 \| 3`，见下表 |
| `memo` | 备忘 |
| `cpa` / `orders` | 数值型业务指标（度支 / 粮饷单） |
| `troops` | **单城**帝国兵力：对应后台关键词数量等业务计数 |
| `equipments` | **单城**军械装备：创意图 / 高级样式等计数 |
| `completedQuestIds` | 当日对该任务至少勘合过一次的模板 `id` 列表（与 **`questDailyCompletions`** 中 count>0 同步，兼容旧档仅列表） |
| `questCompletedAt` | 各任务 id → **最近一次**勘合时间戳（ms）；多次勘合同日会更新为末次 |
| `questDailyCompletions` | 各任务 id → 已勘合次数（1–99）；跨日 **`resetDailyQuests`** 清空非一次性任务键；**`one_time`** 已办条目保留 |

**城池政务与军机联动**：军机处须先选 **主攻城池**（`useQuestStore.activeCityId`）。`toggleQuest(questId)` 在未满 **`maxCompletionsPerDay`** 且体力足够时：**递增**当前城的当日次数、维护 `questCompletedAt`、扣体力、`addExp`（游牧下 **`quest-mva-*`** 功勋 **×1.2** 取整）、再 **`feedDopamineFromQuestReward(expGain)`** 写入多巴胺池并可能铸 **`tokens`**；成功返回 **`ToggleQuestCompletionMeta`**（含铸券数、结算后池值等，供邸报 `revert`）。`toggleQuest(questId, { clearDay: true })` 清空该城该任务**当日**次数（不回退体力/功勋/池与券，与既有约定一致）。`map-store` 内 **`incrementQuestCompletion` / `clearQuestCompletionsForDay` / `decrementQuestCompletion`** 维护计数与 `completedQuestIds`；邸报撤回勘合调用 **`decrementQuestCompletion`**。只读辅助：**`getQuestDailyCount`**、**`isQuestFullyCompletedToday`**（从 `@/store` 或 `map-store` 导出）。

**动作**（补充）：无 `toggleCityQuest`；勘合态以当日计数与阈值为准。

**`status` 语义与 UI 色带**（由 `lib/city-status.ts` 驱动）：`0` 未知之地、`1` 斥候侦查、`2` 战火纷飞、`3` 金色藩属。其中 **`status === 3`** 的沙盘卡片额外使用强金色阴影 + `tailwind` 自定义 **`animate-imperial-gold-glow`**（见 `tailwind.config.ts`），强化「藩属」成就感。

**军备与战力提示**（`lib/city-military.ts` → `isCityEquipmentCritical`）：当 `troops > 0` 且 `equipments === 0`，或 `troops / equipments > 100` 时，中央沙盘城池卡片右上角显示 **红色 `AlertTriangle`**，`Tooltip` 文案：「军备严重不足，战力腰斩，请速派工部打造军械！」

**动作**：

- `addCity(city)`、`removeCity(id)`：增删城池，**无数量上限**。
- `updateCity(id, data)`：`data` 为 `CityPatch`（不可改 `id`），用于朱批侧栏与造办处表单。
- **`bulkAddCities(names: string[])`**：接收名称数组；按名称去重、跳过与现有城池同名项；新城池默认 `status=0`、`cpa=0`、`orders=0`、`troops=0`、`equipments=0` 等；**追加在列表末尾**；返回实际新增数量。名称解析见 **`lib/parse-bulk-city-names.ts`**（支持中文逗号、顿号、英文逗号、空白分隔）。

**持久化键名**：`hanling-map`。无有效存档或首次解析失败时，`merge` 返回 **空疆域 `[]`**。可选用导出方法 `createDefaultCities()` 批量生成示例城列表（含新字段默认值）。

---

### 3.4 `useQuestStore`（军机处 / 分时任务）

**结构**：

- `quests`：任务**模板**数组；每条含 `period`、`title`、`completed`（遗留字段，勘合态以**城池当日计数**为准）、`expReward`、`staminaCost`、**`minCompletionTime`**（最短耗时，分钟）、**`compensationType`**（`absolute` / `compensable`）、**`occurrence`**（`one_time` / `daily_once` / `daily_multiple`）、**`sortOrder`**（同时辰内排序）、**`maxCompletionsPerDay`**（与 `occurrence` 一致：一次性/每日一次为 1，每日多次为 99；造办处可调 1–99）、**`category`**（业务四维）、**`affiliation`**（`city` 分城 / `tongwu` 天下通务）、**`campaignPhase?`**（战役集群阶段）；`id` 稳定。
- `lastLoginDate`：本地日历日 `YYYY-MM-DD`，跨日重置用。
- **`activeCityId`**：`string | null`，军机处 **主攻城池**（通务司或征战目标）；未选择时不可勘合。
- **`activeTimer`**：单机点卯计时（呈报前与集群互斥）。
- **`activeBatchCampaign`**：集团军集群点卯计时（`questId` + `cityIds[]` + 暂停字段）；与 **`activeTimer`** 互斥；持久化于 `hanling-quest`。

**默认模板「祖宗之法」（MVA 三十一务 · 政务清单）**  

`createDefaultQuests()`（`store/quest-store.ts` → `store/default-mva-quests.ts`）提供与 **`docs/政务清单.md`** 对齐的 **31 条固定任务**，按时辰分布为：**早朝 9、晌午 8、傍晚 8、深夜 6**。任务 `id` 形如 **`quest-mva-{时辰}-{序号}`**（如 `quest-mva-早朝-1`），与历史上旧版打包 id **`quest-default-*`** 区分；**仅 MVA 且缺 id 或含未知 id 时整表替换**为当前清单，并 **`pruneQuestProgressForUnknownIds`** 清理各城无效勘合键。

**动作**：

- `addQuest(quest)`、`removeQuest(id)`、`updateQuest(id, data)`：枢密院（造办处）维护任务模板与数值；**无条数上限**。
- **`toggleQuest(questId, opts?)`**：须 **`activeCityId`** 指向有效城池。若 **`opts?.clearDay === true`**：调用 **`clearQuestCompletionsForDay`**，清空该城该任务当日次数（不扣体力）。否则：已达当日上限或体力不足则返回 **`false`**；否则 **`incrementQuestCompletion`** → `consumeStamina` → **`addExp`**（游牧 + **`quest-mva-*`** 时功勋 **×1.2** 取整）→ **`feedDopamineFromQuestReward`**；成功时返回 **`ToggleQuestCompletionMeta`**（非 `boolean`），军机邸报可带完整 **`revert`**（含 **`tokensSubtracted` 为实际铸券数**、`postDopaminePool`、`dopamineExpFed`）供撤回。旧邸报仅有 **`tokensSubtracted: 1`** 且无池字段时，撤回仍按旧逻辑扣券、不调整 **`dopaminePool`**。
- **`setActiveCityId(id)`**：切换主攻城；任务行展示 **`getQuestDailyCount` / `isQuestFullyCompletedToday`** 与 **点卯按钮** 文案（「点卯 / 再办一次 / 已点卯 / 已建功 / 本日已办满」等，依 `occurrence`）。
- `resetDailyQuests()`：跨自然日：更新 `lastLoginDate`，任务表 `completed` 清零；**`useMapStore`** 各城仅保留 **`occurrence === 'one_time'`** 且已有勘合次数的 **`questDailyCompletions` / `questCompletedAt` / `completedQuestIds`**，其余日课键一律清空（邸报 **`logs`** 不随重置清空）。
- **`ensureQuestBootstrap()`**（启动时由 **`EmpireCloudSync`** 在 `rehydrate` 之后调用，见 §2）：**`ensureTongwuSiCity()`**；**`pruneQuestProgressForUnknownIds`**；无效 **`activeCityId`** 置空。**不**自动整表覆盖用户任务列表。造办处或导入密函后亦会调用。旧名 **`ensureMvaQuestCatalog()`** 保留为别名。
- **集团军集群**（见 §3.4.1）：**`startBatchCampaignQuest`** / **`completeBatchCampaignWithTimer`** / **`cancelBatchCampaignTimer`** / **`toggleBatchCampaignTimerPause`**。

**持久化键名**：`hanling-quest`。`partialize` 含 **`activeTimer`**、**`activeBatchCampaign`**。无有效存档时 `merge` 可能得到空任务；默认三十一务由用户主动 **`createDefaultQuests()`** 或造办处导入，而非每次启动强制注入。

#### 3.4.1 集团军 · 战役集群（BatchCampaignOps）

与 **军机处单城点卯** 分离的 **多城流水线**（路由 **`/dashboard/campaign`**，实现 **`components/dashboard/batch-campaign-ops.tsx`**）。

| 步骤 | 行为 |
|------|------|
| A · 战役模式 | 四阶段之一（`lib/campaign-phase.ts`）；列表由 **`filterQuestsByCampaignPhase`** 过滤 |
| B · 参战城池 | 多选 **`getTerritoryCities`**（排除通务司）；展示 **名 + 别名** |
| C · 任务流水线 | 仅 **`affiliation === 'city'`** 的分城政务；每条 **「集群点卯」** |

**开战 `startBatchCampaignQuest(questId, cityIds)`**：筛出可参战城 → **`consumeStamina(staminaCost × n)`** → 写入 **`activeBatchCampaign`**（与 **`activeTimer`** 互斥）。**呈报 `completeBatchCampaignWithTimer`**：按城 **`incrementQuestCompletion`**（已满跳过）→ 合计功勋 **`feedDopamineFromQuestReward`** 一次 → 批量工时奖惩（**T_standard = minCompletionTime × N**，**T_floor = 0.5 × T_standard**）。低于 **T_floor** 作废并退还体力。 **`ON_ORDER`** 且 **开通系统类** 任务成功后触发 **转运粮草** 连锁邸报（每城一条 **`crimsonDecree`**）。

#### 3.4.2 `useGrandTourStore`（巡游四海 · 行在舆图）

| 字段 / 动作 | 说明 |
|-------------|------|
| `atlas` | **舆图库藏**：`provinceGroups`（含 **`macroRegionId`**）+ `regions`（京畿 + `pois`）+ `catalogs`（膳宿 / 驻跸 / 驿传 常供） |
| `tours` / `activeTourId` | 行在列表与当前选中；`GrandTour.status`：`draft` / `active` / `archived` |
| `legs` | `TourLeg[]`：`kind`（含 **`ticket`**）、`period`、`dayIndex`、快照价与文案 |
| `ensureAtlasSeed()` | 首次或旧档：自 **`lib/city-atlas-seed.ts`** 注入 361 市；补全省组 **`macroRegionId`** |
| 密函 | **`replaceAtlasFromBackup`** / **`replaceToursFromBackup`**（`lib/empire-backup.ts` · **`grandTour`**） |

**持久化键名**：`hanling-grand-tour`。`store/rehydrate.ts` 含本 Store。用度由 **`lib/grand-tour-ledger.ts`** 自 legs 汇总（**两**，不扣 **`privateVault`**，M4）。

---

### 3.5 造办处（系统配置 UI）

- **入口**：国库 HUD 右侧 **造办处齿轮**，`Tooltip`「造办处 (系统配置)」，路由 **`/settings`**。
- **实现**：`components/settings/settings-view.tsx`，**Tabs**：「疆域司」「枢密院」「**帝国档案**」；变更即时写入 `persist`（localStorage），CRUD 多伴随邸报。

**帝国档案**：

- **封装帝国密函**：汇总 `emperor`（含 **`dopaminePool`**）/ `map.cities` / `quest`（含 `activeCityId`）/ `events.logs` / 可选 **`grandTour`**（巡游库藏 + 行在）为 JSON 下载，文件名 `hanling-empire-backup-YYYYMMDD.json`（`lib/empire-backup.ts`）。
- **读取帝国密函**：解析后覆盖各 Store（迁移仍走 `migrateCity` / `migrateQuest`；**`grandTour`** 写入 **`hanling-grand-tour`**）。旧密函无 `grandTour` 时不影响其它域。

**疆域司**：

- 单城维护：城池名、产品别名，以及 **CPA、Orders、兵力（`troops`）、军械（`equipments`）** 数字字段（与沙盘侧滑奏折一致）。
- **批量拓土**：疆域司 Tab 顶部 **多行文本框 +「批量拓土」**；解析规则见 `parseBulkCityNamesFromText`；调用 `bulkAddCities`；成功时邸报 **`decree`**：「造办处：圣上威武，帝国版图今日新增 [N] 座城池！」。

**枢密院**：**「分城政务 / 天下通务」** 双 Tab；按四时辰分组维护军机模板（与 §3.4 同一 `quests` 数据源）；每条可编辑 **战役阶段**、**归属**、**业务维度**、**本日勘合上限**、**最短耗时**、**补救**、**时效**。**一键下发大纲**：多行文本解析见 **`lib/parse-bulk-quest-lines.ts`**（`Tab` / `|` / 逗号 / 空白分列；固定尾列自末向前识别，不可跳列）。

---

### 3.6 `useEventStore`（八百里加急 / 邸报）

| 字段 / 动作 | 说明 |
|-------------|------|
| `logs` | 时间倒序；单条含 `id`、`time`、`message`、`type`，可选 **`cityName`**、**`emphasis`**（`calamity` 国难红闪、`goldFlash` 金色、`crimsonDecree` 朱红捷报块） |
| `addLog(message, type?, meta?)` | `type` 默认 `info`；`meta` 可含 `cityName`、`emphasis`、**`revert`**（**`quest_complete`**：含 `tokensSubtracted`、可选 **`postDopaminePool` / `dopamineExpFed`** 以支持多巴胺池回滚） |
| `clearLogs()` | 清空 |
| **`revertLog(id)`** | 若该条含 **`revert`**：调用 **`lib/apply-event-log-revert`**（先校验 **`dopaminePool`** 与记录一致再 **`decrementQuestCompletion`**）+ **`revertQuestCompletionEffects`**；成功后从列表移除。失败原因含 `state_mismatch`（勘合已变/跨日清空/池不一致）、`city_missing` 等；实现可向 Console 输出 **`[Hanling]`** 前缀便于排查 |

**邸报 UI（`components/dashboard/event-log-docket.tsx`）**：

- 仅当 **`log.revert` 存在** 时显示**金色描边「撤回」按钮**；否则显示 **「— 无可撤回」** 文案（**非**「禁用按钮」：旧版禁用态带 `pointer-events-none`，易被误认为坏交互）。
- **是否显示「撤回」与「隔了多久」无关**；无「几分钟后自动不可撤」的 UI 逻辑。跨日后当日勘合已清空时，**仍可能显示**「撤回」按钮（`revert` 仍在 JSON 中），但点击会 **失败** 并提示 **state_mismatch**。
- 面板标题区展示 **「本列表可撤回：N 条」** 统计与上述说明。

**容量**：**云端 `event_log` 长期保留**；本地 `hanling-event` 环形缓存约 **500** 条（`MAX_LOCAL_CACHED_LOGS`）。**`EventLogPanel`** 顶栏卷轴默认仅展示 **北京时间今日** 邸报（最多 **80** 条渲染），往期请至 **勤政录 → 邸报摘录**。

**云同步**（已登录）：`addLog` / `revertLog` → **`lib/activity-journal-bridge.ts`** → **`upsertEventLogToCloud` / `deleteEventLogFromCloud`**（`lib/supabase/activity-journal-sync.ts`）。

**持久化键名**：`hanling-event`（本地缓存，非权威）。

---

## 4. 跨模块联动（当前实现）

| 场景 | 行为 |
|------|------|
| 军机处勘合（主攻城已选） | `toggleQuest` → `map-store` 当日计数递增 + 体力/功勋 + **多巴胺池铸 `tokens`**；邸报可带完整 **`revert`**、`cityName` |
| 集团军集群点卯 | **`/dashboard/campaign`** → **`startBatchCampaignQuest`** / **`completeBatchCampaignWithTimer`**；多城计数 + 合计 **`feedDopamineFromQuestReward`**；**开通系统** 破城连锁邸报 |
| 尊号跨阶晋升 | `addExp` 及御苑等直接加功勋路径：比较前后功勋阶梯，命中则 **`addLog(..., { emphasis: 'crimsonDecree' })`** |
| 养正司 / 内务府动作 | `emperor-store` 内多数直接 **`addLog`**（太医院、户部、理藩、宗人等） |
| 沙盘奏折保存（`WarMap` 朱批） | 若 **CPA 较原值上调**，按差额从 **`useEmperorStore.gold`** 扣减（**不低于 0**），并写 **`treasury`** 邸报说明支用银两；朱批 **`decree`** 照常 |
| 户部司帑 | 顶栏点击 **国库** → Dialog：**岁入 `injectGold`** / **校准 `syncGold`**，均写邸报（§3.2） |
| 应用启动 | **`EmpireCloudSync`**：`rehydrateAllStores` →（已登录）拉 **`user_empire`** + **`event_log` / `quest_work_session`** → `resetDailyQuests` → **`ensureQuestBootstrap`**（§2） |
| 军机点卯 / 集团军计时 | **`work-session-recorder`** 写 **`quest_work_session`**；叙事仍 **`addLog`** → **`event_log`** |
| 批量拓土 / 枢密院变更 / 帝国档案导入导出 | 见 §3.5 与 `empire-archive-panel`；密函 **`grandTour`** ↔ 云 **`grand_tour_json`** + **`hanling-grand-tour`** |
| 巡游四海行在 | **`/dashboard/grand-tour`**；库藏与行在分离、leg 快照；**不**自动扣 **`privateVault`**（M4）；与城池 **巡幸疆土** 无数据互通 |

**尊号与等级**：HUD 上位阶 + 尊号由 **`lib/emperor-title.ts`** 按 **累计 `exp` 的「九品中正」二十五阶尊号阶梯**（布衣至超凡，正品 / 从品交错）展示；**位阶内进度条**与「距下一阶还需 n 功勋」由 **`getEmperorTitleProgress`** 派生。Store 内数值型 **`level`**（`floor(exp/100)+1`）仍为养成刻度，与尊号阶梯**相互独立**。

| 功勋（exp）区间（左闭右开，末阶无上界） | 位阶（`level`） | 尊号（`title`） |
|----------------------------------------|-----------------|----------------|
| [0, 2000) | 草根 | 布衣·白身 |
| [2000, 6000) | 从九品 | 九品·芝麻官 |
| [6000, 15000) | 正七品 | 县令·牧民 |
| [15000, 40000) | 从五品 | 知府·封疆 |
| [40000, 100000) | 从三品 | 巡抚·节度 |
| [100000, 250000) | 正一品 | 辅政·宰执 |
| [250000, 600000) | 开国公 | 封侯·拜将 |
| [600000, 1200000) | 潜龙 | 问鼎·郡王 |
| [1200000, 3000000) | 亲王 | 监国·太子 |
| [3000000, 10000000) | 昭昭 | 九五·至尊 |
| [10000000, 50000000) | 万世 | 瀚翎·大帝 |
| [50000000, ∞) | 超凡 | 潜龙·天帝 |

---

## 5. UI 信息架构

整体由 **`components/dashboard/dashboard-shell.tsx`** 拼装。

1. **顶部吸顶：国库 HUD（`TreasuryHUD`）**  
   - **左侧**：位阶名（muted）+ **尊号**（`imperial-gold` 高亮）+ `Lv` 与累计功勋；其下为距下一尊号所需功勋（巅峰阶为已满阶说明）；**全宽主进度条**映射 **当前位阶内** 功勋占比（非全生涯总长）。实现见 **`lib/emperor-title.ts`** + **`getEmperorTitleProgress`**。  
   - **中部**：`EmpireBriefingStrip` — 疆域座数、军机务数、粮饷单合计、度支 Σ；若 **`isNomadMode`** 则显示 **「⛺ 移动行宫」** 徽章（提示 MVA 功勋加成）。  
   - **右侧**：**国库银两**（可点击打开 **户部司帑** Dialog：岁入登记 / 现金校准）、帝国军力摘要、**八百里加急（邸报）**卷轴、**勤政录（日历）**→ **`/dashboard/activity`**、**巡游四海（Compass）**→ **`/dashboard/grand-tour`**、**集团军（Layers）**→ **`/dashboard/campaign`**、**造办处**齿轮（`/settings`）。  
   - **不再**在 HUD 展示龙体/体力条（已迁入下方 **内务府** 奏折区）。

2. **主区：中央沙盘（`WarMap`）**  
   - 响应式网格：`2` → `md:4` → `xl:5` 列。  
   - 标题区 **态势多选筛选条**（四类名称 Chip，默认「战火纷飞」；全取消=不筛选）；网格仅渲染可见态势城池。  
   - 卡片：名称、态势、`CPA/单/兵/械`、备忘摘要；军备临界 **AlertTriangle**（§3.3）。  
   - **邸报捷报**：`OracleBriefing` 等（若有）。  
   - **Sheet 奏折**：别名、疆域态势、**建设进度**（四部：`lib/quest-imperial-axis.ts`，以 **`isQuestFullyCompletedToday`** 判定条目标满；下钻展示当日 **n/max** 与末次勘合时间）、`memo`、数值字段与朱批保存。**保存时若 CPA 上调**：从皇帝 **`gold`** 按差额支用（见底为 0），见 §4「沙盘奏折保存」。  
   - **集团军**：**不在**主区嵌入；见子路由 **`/dashboard/campaign`**（§3.4.1）。

3. **右侧栏（大屏 ≥ lg）：军机处 | 养正司 | 内务府**（同一 `aside` 内横向分栏）  
   - **`QuestEngine`**：顶栏 **主攻城池**（**通务司置顶** + 征战目标；选通务司仅见天下通务政务，选征战目标仅见分城政务；**点卯/集群计时中不可换城**）；**`lg` 及以上** 用 `Select`（下拉内搜索）；**小屏** 底栏 Sheet。按时辰分组 + 业务维度筛选；每务 **「点卯 / 再办一次 / 本日已办满」**；游牧 MVA +20%。  
   - **`InnerCourtCard`（养正司）**：`components/inner-court.tsx` — 太医院 / 户部度支 / 理藩院（**开卷舆图 · 巡游四海**、国子监/太庙、网红打卡、**移动行宫**）/ 宗人府。  
   - **`NeiwufuCabinet`（内务府）**：皇帝圣躬（健康/体力条、危急闪烁 `animate-imperial-vitals-alert`）、武术、朝服/宵禁、御苑翻牌（**翻牌券 + 多巴胺蓄池条 + 券数回弹动画**）、校场等。

4. **小屏（< lg）**  
   - 底栏按钮 **「军机处 · 养正与宫务」** 打开 **Sheet**（`sr-only` 标题「军机处 · 养正司与内务府」）；内为军机 +（养正司叠于内务府上）纵向滚动；主区 `pb` 防遮挡。

5. **全局叠加**  
   - **`ImperialRedLineWatch`**：红线规则与邸报联动（若存在）。  
   - **邸报（八百里加急）**：顶栏 **卷轴** → **`EventLogPanel`**；**默认仅北京时间今日**；时间 **`YYYY-MM-DD HH:mm:ss`**；**撤回**规则见 §3.6；清空仅清本地缓存列表。  
   - **勤政录**：顶栏 **日历** → **`/dashboard/activity`**；**邸报摘录** / **政务工时** 双 Tab；日/周/月 + Markdown/JSON 导出（JSON 含工时行）；子页 **`MobileSubpageShell`**（小屏全宽筛选与导出）。
   - **巡游四海**：顶栏 **Compass** → **`/dashboard/grand-tour`**（行在规划，见文首「巡游四海」）；**`< lg` 行在目录 Sheet**。
   - **集团军 · 战役集群**：顶栏 **Layers** → **`/dashboard/campaign`**（多城批处理，见 §3.4.1）；统一子页壳 + 触控优化。

6. **独立子路由（小屏）**  
   - 勤政录 / 巡游 / 集团军 / 造办处均 **非** 主页三栏布局；顶栏 **返回九州** + **安全区** + **≥44px** 主操作（见文首「移动端 H5 · 子页面 M1」）。造办处 **`/settings`** 顶栏与 Tab 已做窄屏适配；帝国手册 **`/settings/handbook`** 沿用造办处导航习惯。

---

## 6. 视觉与主题

- **基底**：`app/layout.tsx` 根节点 `className="dark"`，整体为**深色模式**。
- **皇家主题色**（Tailwind 扩展 `imperial`）：  
  - **暗金** `#F59E0B` → `imperial-gold`，用于尊号点缀、国库意象、主操作强调等。  
  - **朱红** `#E11D48` → `imperial-vermilion`，用于军力、战火态势、邸报标题等。
- **shadcn 语义色**：`globals.css` 中 `--primary` 偏金、`--accent` 偏朱红，与组件默认 variant 一致。
- **城池卡片**：背景与边框随 `status` 变化；**藩属（3）** 叠加强金阴影与呼吸流光动画（见 §3.3）。沙盘区块外层可使用 **`TooltipProvider`** 以支持军备警告的悬浮说明。

**军机处按钮态**：

- 未满档：**点卯** 或 **再办一次**；已满档：**本日已办满** / **已点卯**（`daily_once`）/ **已建功**（`one_time`）且禁用；数据以 **`questDailyCompletions`** 与 **`maxCompletionsPerDay`** 为准。

**圣躬危急**（`tailwind.config.ts` + `neiwufu-cabinet.tsx`）：健康 **< 30** 或体力 **< 40** 时，龙体/御体条启用 **`animate-imperial-vitals-alert`** 闪烁（与 `lib/imperial-vitals.ts` 宵禁/早朝阈值一致）。

---

## 7. 与业务文档的关系（建议）

- **`docs/MVA.md`**：可继续作为「动作设计、复盘节奏」的**事实来源**。仪表盘内 **军机处默认三十一务** 已由 `createDefaultQuests()` 与四时辰政务清单对齐（实现见 `store/default-mva-quests.ts` / `store/quest-store.ts`）；字段与启动迁移见本文 **§3.4**。
- **`docs/锚定.md`**：可作为个人原则 / 决策锚点；若未来 HUD 需要展示「当日锚点」，可从该文档提炼只读字段（需另行设计与 Store 字段）。

---

## 8. 已知局限与可扩展方向（非承诺）

- 军机「`clearDay`」不回退体力/功勋/多巴胺池与券；**邸报撤回**可回滚单次勘合（体力、功勋、**按记录扣回 `tokens`**、**`dopaminePool`**（若邸报含池字段）+ **`decrementQuestCompletion`**）。若需其它可逆路径，再扩展对称扣减与校验。
- **未登录**时邸报/工时仅本地；**已登录**时权威在 Supabase（**无 Realtime**，他端需刷新/重登）。**帝国密函**（`version` 1/2）仍建议作离线备份；勤政录 JSON 导出可含 **`workSessions`**。
- 城池编辑为右侧 **Sheet**；造办处已支持 **批量拓土**；表格化批量改指标可再扩展。
- 尊号「九品中正」二十五阶与数值型 `level`（每 100 功勋一级）未强制绑定；若需统一可改为单一派生。

---

## 9. 文档维护

| 项目 | 建议 |
|------|------|
| 本文档路径 | `docs/瀚翎帝国-设计与机制说明.md` |
| 何时更新 | 变更 Store 字段、持久化键、`reset`/`toggle`/`ensureQuestBootstrap`、**集团军集群**、**巡游四海**、**勤政录**、**移动端 H5**、沙盘筛选、布局断点或主题变量时 |
| 版本 | 与仓库当前实现同步；大改版可在文首增加「近期修订」小节 |

---

*文末与实现对齐：`hanling-emperor` / `hanling-map` / `hanling-quest`（**`clientSessionId`** 计时）/ `hanling-event`（本地缓存）/ **`hanling-grand-tour`** / `hanling-prefs`；云表 **`user_empire`**（含 **`grand_tour_json`**）、**`event_log`**、**`quest_work_session`**；**`EmpireCloudSync`** + **`activity-journal-bridge`**；子页 **`/dashboard/activity`**（勤政录双 Tab）、**`/dashboard/grand-tour`**、**`/dashboard/campaign`**；**`MobileSubpageShell`** + **`lib/mobile-ui`**（子页面 H5 M1）。*
